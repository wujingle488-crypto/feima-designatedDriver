<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fmdj.mis.api.db.dao.DeptDao">

    <select id="selectDeptByPage" resultType="java.util.HashMap" parameterType="map">
        SELECT d.id,
        d.dept_name AS deptName,
        d.tel,
        d.email,
        d.`desc`,
        COUNT(u.id) AS emps
        FROM tb_dept d
        LEFT JOIN tb_user u ON u.dept_id = d.id AND u.`status` = 1
        WHERE 1=1
        <if test=" deptName!= null ">
            AND d.dept_name LIKE '%${deptName}%'
        </if>
        GROUP BY d.id
        LIMIT #{start}, #{length}
    </select>

    <select id="selectDeptCount" resultType="long">
        SELECT COUNT(1) FROM (
        SELECT d.id
        FROM tb_dept d LEFT JOIN tb_user u ON u.dept_id=d.id AND u.status=1
        WHERE 1=1
        <if test="deptName!=null">
            AND d.dept_name LIKE '%${deptName}%'
        </if>
        GROUP BY d.id
        ) AS temp
    </select>

    <select id="selectAllDept" resultType="java.util.HashMap">
        SELECT
            id,
            dept_name AS deptName
        FROM tb_dept
        GROUP BY id
    </select>

    <select id="selectById" resultType="java.util.HashMap" parameterType="int">
        SELECT id,
               dept_name AS deptName,
               tel,
               email,
               `desc`
        FROM tb_dept
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.fmdj.mis.api.db.pojo.DeptEntity">
        INSERT INTO tb_dept
        SET dept_name = #{deptName}
        <if test="tel!=null">
            ,tel=#{tel}
        </if>
        <if test="email!=null">
            ,email=#{email}
        </if>
        <if test="desc!=null">
            ,`desc`=#{desc}
        </if>
    </insert>

    <update id="update" parameterType="com.fmdj.mis.api.db.pojo.DeptEntity">
        UPDATE tb_dept
            SET dept_name = #{deptName},
                tel=#{tel},
                email=#{email},
                `desc`=#{desc}
        WHERE id= #{id}
    </update>

    <select id="hasAssociatedUsers" resultType="boolean">
        SELECT IF(SUM(temp.users)>0,true,false) AS result
        FROM (SELECT COUNT(u.id) AS users
        FROM tb_dept d
        JOIN tb_user u ON u.dept_id = d.id
        WHERE d.id IN
        <foreach collection="array" open="(" separator="," close=")" item="one">
            #{one}
        </foreach>
        GROUP BY d.id
        ) AS temp
    </select>

    <delete id="deleteDeptByIds">
        DELETE FROM tb_dept
        WHERE id IN
        <foreach collection="array" open="(" separator="," close=")" item="one">
            #{one}
        </foreach>
    </delete>
</mapper>