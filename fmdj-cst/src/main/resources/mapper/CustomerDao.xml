<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fmdj.cst.db.dao.CustomerDao">

    <!--  乘客注册  -->
    <insert id="registerNewCustomer" parameterType="Map">
        INSERT INTO tb_customer
        SET open_id = #{openId},
            nickname = #{nickname},
            sex = #{sex},
            photo = #{photo},
            tel = #{tel},
            `status` = 1
    </insert>

    <!--  判断乘客是否存在  -->
    <select id="hasCustomer" parameterType="Map" resultType="long">
        SELECT COUNT(id) AS ct
        FROM tb_customer
        WHERE 1=1
        <if test="openId!=null">
            AND open_id = #{openId}
        </if>
        <if test="customerId!=null">
            AND id = #{customerId}
        </if>
    </select>

    <!--
    根据customerID查询乘客信息
    CAST：类型转换的参数
      -->
    <select id="selectCustomerIdByOpenId" parameterType="String" resultType="String">
        SELECT CAST(id AS CHAR) AS id
        FROM tb_customer
        WHERE open_id = #{openId}
    </select>

    <!--  乘客登录的方法，通过微信登录即可 -->
    <select id="login" parameterType="String" resultType="String">
        SELECT CAST(id AS CHAR) AS id
        FROM tb_customer
        WHERE `status` != 2 AND open_id = #{openId}
    </select>



</mapper>
